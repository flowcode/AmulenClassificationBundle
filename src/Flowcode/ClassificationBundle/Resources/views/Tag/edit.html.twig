{% extends 'FlowcodeDashboardBundle:Default:layout.html.twig' %}

{% block body -%}
{{ include('FlowcodeClassificationBundle:Tag:tagHeader.html.twig') }}
<section class="content">
    <div class="col-sm-6">
        <div class="box box-solid">
            <div class="box-header">
                <h3>{{ "edit page"|trans|title }}</h3>
                <div class="box-tools">
                    <a href="{{ path('admin_tag_show', { 'id': entity.id }) }}">
                        {{ "Back"|trans }}
                    </a>
                </div>
            </div>
            <div class="box-body">
                {{ form(edit_form) }}
                {{ form(delete_form) }}
            </div>
        </div>
    </div>
</section>
{% endblock %}
